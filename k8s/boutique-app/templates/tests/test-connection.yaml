apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "boutique-app.fullname" . }}-test-connection"
  labels:
    {{- include "boutique-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "boutique-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
