apiVersion: v1
kind: Service
metadata:
  name: {{ include "boutique-app.fullname" . }}
  labels:
    {{- include "boutique-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "boutique-app.selectorLabels" . | nindent 4 }}
